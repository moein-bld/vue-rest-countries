{
	"name": "alibaba-monorepo",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"client": "pnpm --filter @app/client",
		"build:client": "pnpm --filter @app/client build",
		"build:lib": "pnpm --filter @pack/ui-lib build",
		"dev": "pnpm build:client && pnpm --recursive --parallel --stream run dev"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"prettier": "^2.8.8",
		"typescript": "^5.0.2"
	}
}
